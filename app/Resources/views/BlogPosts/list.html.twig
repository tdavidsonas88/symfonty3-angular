{% extends '::base.html.twig' %}

{% block body %}
    <table class="table table-hover table-responsive">
        <thead>
            <tr>
                <th>id</th>
                <th>Title</th>
                <th>Options</th>
            </tr>
        </thead>
        <tbody>
            {% for post in blog_posts %}
            <tr>
                <td>{{ post.id }}</td>
                <td>{{ post.title }}</td>
                <td>
                    <a href="{{ path('edit', { 'blogPost': post.id }) }}" class="btn btn-primary btn-sm">Edit</a>
                    <a href="#" class="btn btn-danger btn-sm">Delete</a>
                </td>
            </tr>   
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
